guard 'coffeescript', :output => 'lib', :bare => true, :all_on_start => false do
  watch(%r{src/(.+\.coffee)})
end
guard 'coffeescript', :output => 'spine', :bare => true, :all_on_start => false do
  watch(%r{spine-src/(.+\.coffee)})
end